package com.kale.interview;

import org.junit.jupiter.api.Test;

class ContextLoadsTest extends BaseIntegrationTest {

    @Test
    void contextLoads() {
        // Asserts only that the Spring context boots cleanly, the container
        // starts, and Flyway runs against it. No further assertions.
    }
}
